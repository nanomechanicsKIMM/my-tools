# Patent strategy report skill – Python scripts
# Main workflow uses only pandas (user provides CSV from Google Patents).
pandas>=2.0.0

# For abstract crawling and relevance scoring
requests>=2.28.0
beautifulsoup4>=4.12.0
scikit-learn>=1.2.0

# Playwright (not used – CSV is downloaded manually by the user)
# playwright>=1.40.0
