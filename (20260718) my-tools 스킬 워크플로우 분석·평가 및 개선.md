---
title: my-tools 스킬 워크플로우 분석·평가 및 개선
created: 2026-07-18
tags:
  - skill-review
  - my-tools
  - workflow
  - 크로스플랫폼
---

# my-tools 스킬 워크플로우 분석·평가 및 개선 (2026-07-18)

> [!info] 분석 방법
> 스킬 20종 + 플러그인 5종 + 커맨드 9종을 4개 그룹(특허/논문·학술/행정·HWPX/기타·인프라)으로 나눠 병렬 심층 분석(에이전트 4기). 사용자 주 업무(특허 창출→출원→방어, 논문, 연구행정 HWPX) 기준으로 워크플로우 커버리지와 스킬 간 연계를 평가. **이 세션에서 P0·P1 개선 26건을 즉시 적용 완료**(§4), 장기 과제는 로드맵(§5)으로 분리.

## 1. 총평

| 영역 | 성숙도 | 평가 |
|------|:---:|------|
| 특허 (incubation·draft-review·defence·strategy) | ★★★★★ | 창출→검토→방어→전략 전 단계 커버. incubation은 이중 critic + 기계 게이트로 레포 최고 수준. 7/5 개선전략 문서의 H 항목 대부분 반영 완료 확인 |
| 논문·학술 (9스킬+9커맨드) | ★★★★☆ | 수집→변환→그림→리뷰 강력. 단 자작 스킬(국문 MD)과 KatmerCode 커맨드(영문 HTML) 두 계보가 서로 호출하지 않음 |
| 연구행정 HWPX | ★★★★☆ | 템플릿·워크플로우 설계 우수(8개 템플릿 전부 실존 확인). HWPX 엔진 스크립트가 3벌 중복 |
| 인프라 (setup·bootstrap) | ★★★☆☆ | setup 견고. 단 flat 복사 배포가 스킬 간 절대경로 결합의 근원 |

**관통 결론**: 개별 스킬 품질은 높으나 ① 어제 patent-incubation에 적용한 크로스플랫폼 패턴이 나머지 스킬에 전파되지 않아 macOS에서 4개 스킬이 실행 불가였고(→ 금일 전량 수정), ② 파이프라인 연계가 "문서상 표"일 뿐 실행 데이터가 흐르지 않으며, ③ 품질 규칙 일부가 기계 게이트 없이 육안 검증에 의존한다.

## 2. 주요 발견 (수정 전 상태)

### P0 — 실행 불가·정책 충돌 (전량 금일 수정 완료)

1. **트리거 불능 2건** — `patent-incubation-interactive`·`nrf-tech-survey` SKILL.md에 YAML frontmatter 자체가 없어 스킬 매칭 엔진에 description이 전달되지 않았음. interactive는 사실상 죽은 스킬 위험(모든 특허 요청이 auto로만 라우팅). *확인됨*
2. **macOS 실행 불가** — `patent-draft-review/scripts/hwpx_to_md.py`(HWPX 입력 100% 실패, 직접 재현), `nrf-tech-survey/scripts/build_survey.py`(hwpx-xml 엔진 4개를 `C:/Users/JHKIM/...` 절대경로 참조), `paper-pdf-download/get_paper.sh`(python 기본값이 Windows miniconda), `pdf-to-md/pdf_to_md.py`(java 폴백). *확인됨*
3. **존재하지 않는 경로 참조** — hwpx 스킬 SKILL.md가 레포에 없는 `C:/Users/JHKIM/gonggong_hwpxskills/` 경로를 5회 안내(예시 그대로 실행 시 실패). *확인됨*
4. **정책 자기모순: Sci-Hub** — `paper-review`가 자체 스크립트로 sci-hub.vg를 3순위 다운로드 경로로 사용 — 같은 레포 `paper-pdf-download`의 "페이월 우회 금지" 원칙과 정면충돌. 공개 레포 + 기관 소속 계정이라 리스크가 큼. *확인됨*
5. **개인정보 위생** — 공개 GitHub 레포 전반에 개인 사용자명(`C:/Users/JHKIM`) 경로 42개 파일 잔존. *확인됨*

### P1 — 드리프트·트리거 품질

6. `~/Claude_Work` vs `~/Claude_work` 대소문자 혼용(runtime 스크립트 포함, case-sensitive FS에서 한쪽 실패). 참고: 이 Mac에는 두 폴더 모두 부재 → **KIPRIS 키 미설정 상태, 선행조사가 항상 degraded로 동작 중** (추정: 키가 다른 위치에 있을 수 있음 — 확인 필요).
7. 죽은 참조: `review-claims` 스킬(실존하지 않음, incubation 2종에서 호출 안내), `PATENT_STRATEGY_SKILL` 미사용 상수.
8. `patent-strategy-report` vs `patent-strategy-pro` 트리거 중복("특허 전략 보고서") — 라우팅 모호.
9. 문서-실물 불일치: overseas-trip-plan "tor office/ 재사용" 선언(실제는 자체 복사본), hwpx-xml junction 구조 설명(Windows 개발기 전용), tor 폐기된 작업 디렉토리 지시.
10. **설치본-레포 비대칭**: 심링크는 incubation 2종뿐, 나머지는 복사본이라 레포 수정이 실행에 반영 안 됨(재설치 필요). *확인됨*

### P2 — 구조·장기

11. HWPX 엔진 스크립트(`office/unpack·pack.py`, `fix_namespaces.py`, `validate.py`) 최소 3벌 중복(tor·overseas·hwpx-xml) — 단일 공용 계층 승격 필요.
12. incubation SKILL.md 1104/1276줄 과다 지침 — JSON 스키마가 SKILL.md와 agents에 이중 기재(SSOT 위반).
13. interactive에 auto와 완전 동일한 복제 9파일 + 의도적 fork 7파일의 스키마 drift(phase2 JSON 필드명 상이, phase5 필드 누락).
14. patent-strategy-report: 실험 스크립트 16개 난립(하드코딩 7개), 특정 RFP 산출물 30개 오커밋.
15. 두 계보 미연계: KatmerCode 커맨드(문헌 발견)의 DOI가 paper-pdf-download(수집)→pdf-to-md(변환)→Zettelkasten으로 흐르지 않음.
16. mineru-tools 플러그인 orphan(plugin.json 없음, marketplace 미등록).

## 3. 파이프라인 커버리지 맵

### 특허 (핵심 업무)
```
창출(incubation) ✅✅ ─▶ 검토(draft-review) ✅ ─▶ 출원 ─▶ 방어(defence) ✅ ─▶ 전략(strategy×2) ✅
```
빠진 고리: ① 창출→검토 자동 인계 없음 ② 검토↔방어 데이터 재사용 없음(금일 상호참조 문서화로 1단계 완화) ③ **방어→창출 폐루프 미구현**(거절 패턴을 다음 발명의 청구항 독트린에 환류 — 가장 가치 큰 미완 고리) ④ 전략 landscape와 incubation Phase 5가 같은 KIPRIS 데이터를 이중 수집.

### 논문
```
①문헌발견 ✅(커맨드) ②수집 ✅ ③MD변환 ✅ ④Zettelkasten 🟡 ⑤집필 🟡 ⑥그림 ✅✅ ⑦리뷰 ✅✅ ⑧투고 🟡
```
빠진 고리: ①→②→④ 자동 흐름(커맨드 결과가 HTML에 갇힘), 본문 드래프팅(의도적 공백일 수 있음), cover letter·저널 포맷팅.

### 행정 HWPX
구매요구서 ✅ · 출장계획서 ✅ · TOR ✅ · NRF수요조사 ✅. 빠진 문서(수요 순): **국외출장 귀국보고서**(출장계획서 데이터 재활용 가능) > 검수조서/납품확인서(구매 짝) > 연차·최종 실적보고서 > 여비정산.

## 4. 금일 적용한 개선 (26건)

### 트리거·라우팅
- [x] interactive·nrf-tech-survey SKILL.md에 YAML frontmatter 신설(구분 트리거 명시, auto↔interactive 라우팅 기준 포함)
- [x] strategy-report SKILL.md에 pro와의 역할 분담 명시(README 라우팅 표에도 반영)
- [x] defence에 "관련 스킬" 섹션 신설(draft-review 이력 재사용 지침 포함)

### macOS 실행 복구 (스크립트 8건, 문법 검증 통과)
- [x] `nrf-tech-survey/build_survey.py` — hwpx 엔진 4경로를 `Path.home()` 기반 + 부재 시 명시적 에러
- [x] `patent-draft-review/hwpx_to_md.py` — HWPX_XML_SCRIPTS를 `Path.home()` 기반
- [x] `pdf-to-md/pdf_to_md.py` — java 폴백을 `~/miniconda3/{Library/bin,bin}` 일반화
- [x] `paper-pdf-download/get_paper.sh`·`dlpaper.sh` — python3 PATH 우선 + Zettelkasten 라이브러리 D:/ 부재 시 `~/Zettelkasten/References` 폴백
- [x] `svg2emf.py` ×2 — 개인 사용자명 경로를 `expanduser`로
- [x] `mineru-tools/convert_mineru.py` — `shutil.which("mineru")` 우선 탐지

### 정책·위생
- [x] **paper-review Sci-Hub 전면 제거** — `try_scihub()` 삭제, 실패 시 paper-pdf-download 스킬 위임으로 교체(SKILL.md·스크립트 동기 수정)
- [x] 공개 레포 개인경로 `C:/Users/JHKIM` 42개 파일 전량 제거(`~`·`$HOME`·`Path.home()`으로; OS 설명 주석 2곳만 의도적 유지)
- [x] `Claude_work`→`Claude_Work` 통일(_shared 스크립트·phase6c·defence 등 10곳)
- [x] hwpx SKILL.md의 gonggong_hwpxskills 사경로 5곳 → 배포 경로 기준 예시로 재작성
- [x] 죽은 참조 제거: review-claims ×2, PATENT_STRATEGY_SKILL 상수
- [x] 문서 drift 3건 수정(overseas office/ 서술, hwpx-xml junction, tor 작업 디렉토리)
- [x] miniconda 하드코딩 python 명령 → `python3` (defence·paper-review·purchase-requisition·abstract-evaluation·pdf-to-md)

### 문서
- [x] **README.md 전면 재작성** — 스킬 4종만 기재돼 있던 것을 20스킬+5플러그인+9커맨드 업무영역별 카탈로그로 교체(기능 + "이렇게 말하면 실행" 사용법, 파이프라인 다이어그램, 의존성 표, 라우팅 정책)
- [x] 설치본 재배포(`~/.claude/skills`·`~/.codex/skills`)로 복사본 drift 해소

## 5. 남은 로드맵 (권고)

**1순위 — 사용자 확인 필요**
- [ ] KIPRIS 키: 이 Mac에 `~/Claude_Work/.env` 생성(현재 부재 → 선행조사 degraded). Windows 실폴더 대소문자도 `Claude_Work`인지 확인
- [ ] 설치 정책 결정: 자주 개발하는 스킬(defence·draft-review·strategy-report)도 심링크로 전환할지 (현재 incubation 2종만 심링크)
- [ ] patent-strategy-report 정리: 실험 run_*.py 16개 legacy/ 분리, 특정 RFP 산출물 30개·test CSV 레포에서 제거 (삭제 수반이라 미실행)

**2순위 — 구조 개선**
- [ ] HWPX 공용 엔진 계층: office/unpack·pack·fix_namespaces·validate·table_utils를 hwpx-tools 단일 소스로 승격(3벌 중복 해소)
- [ ] interactive 복제 9파일 심링크화 + fork 7파일 JSON 스키마 통일
- [ ] 기계 게이트 신설: `svg_lint.py`(도면 텍스트 겹침), `check_refnum_consistency.py`(§6↔§8 구성요소 명칭 대조) — 7/5 분석의 미실행 H 항목
- [ ] 커맨드→스킬 파이프 연결: /lit-search·/research-gap 결과 DOI → paper-pdf-download → pdf-to-md → vault 원커맨드화

**3순위 — 신규 가치**
- [ ] **defence→incubation 폐루프**: 거절 패턴·심사관 논거를 user-philosophy·청구항 독트린에 자동 환류
- [ ] 국외출장 귀국보고서 스킬(출장계획서 manifest 재활용)
- [ ] TRIZ 방법론 심화 reference 5종(기능분석/CECA·76표준해·자원분석·TESE·claim 매핑) — 7/5 분석 §2
- [ ] incubation Phase 5 ↔ strategy landscape KIPRIS 데이터 캐시 공유

## 6. 검증 상태

- **확인됨**: 하드코딩 경로 전수(grep, 수정 후 0건 재확인), frontmatter 부재, 수정 스크립트 8건 `py_compile`/`bash -n` 통과, 템플릿 자산 8종 실존, hwpx_to_md macOS 실패(에이전트 직접 재현), 설치본 심링크 비대칭(ls -la), Sci-Hub 코드(소스 확인 후 제거).
- **추정**: 트리거 임베딩 매칭 개선 효과(frontmatter 추가가 라우팅을 개선한다는 것은 스킬 시스템 동작 방식에서 추론), KIPRIS 키의 실제 위치(가정: 미설정).
- **미실행**: 수정된 스킬들의 end-to-end 실행 테스트(HWPX 생성 등은 실입력 필요) — 다음 실사용 시 첫 실행 결과 확인 권장.

## 관련 문서
- [[(20260705) patent-incubation 스킬 개선방안 분석]] — 특허 스킬 심층(대부분 반영 완료)
- `docs/특허창출스킬_궁극개선전략.md` — P0 백포트 완료 확인
